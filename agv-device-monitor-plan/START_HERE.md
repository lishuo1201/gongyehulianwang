# 从这里开始：把计划交给 Codex

版本：1.2｜更新日期：2026-09-12｜语言：中文

本项目面向已有 **7 年 Java 互联网开发经验**、希望补足工业领域知识并应聘工业软件 Java 后端岗位的开发者。学习覆盖完整工业协作链路，工程保持 AGV 模拟设备只读采集监控；实际功能进度以任务交接为准。你负责设计、推理和回答，Codex 负责编码及运行测试，亲手编码为可选练习。每天 2～4 小时、五天 MVP 与十天完善仅作工程预算。

先读 [工业系统全景与产线补料案例](docs/02-architecture.md)，再按 [开发与学习计划](docs/05-development-plan.md)从 [任务交接](docs/06-tasks.md)的唯一待答问题继续。Java 基础按需复习，陌生工业概念先讲背景再做场景判断；[面试训练](docs/09-interview-guide.md)从当前阶段开始，T20 汇总。

## 1. 如何放进项目

1. 解压文件包。可以直接把 `agv-device-monitor-plan` 文件夹重命名为 `agv-device-monitor`，作为一个新项目。
2. 如果已有项目，把本包内容合并到项目根目录。已有 `AGENTS.md`、`README.md` 或同名文档时先比对，不能直接覆盖。
3. 在这个项目目录打开 Codex，把下面的首次任务发给它。
4. 每轮查看工程结果和学习记录；当前任务工程完成、学习通过后，才进入下一个依赖满足的任务。

计划文档包含 14 个 Markdown 文件：根目录 3 个，`docs/` 目录 11 个。T01 已新增 `backend/`、`simulator/`、父 POM 和 Maven Wrapper。实际进度以 `docs/06-tasks.md` 为准。

## 2. 第一次发给 Codex 的任务

以下是尚未实现 T01 时的首次开发模板；当前目录已有 T01 骨架，请使用第 3 节先补学习验收，不重复创建骨架。

```text
请按本项目的文档开始 AGV 模拟设备监控 Demo 的开发与学习。

我已有 7 年 Java 互联网开发经验，目标是工业软件 Java 后端。
请补工业领域前置知识，Java/Maven/Spring/CRUD 基础按需复习，不默认设工具链考试。
完整讲解工业链路，但保持只读监控工程范围。

先读取 AGENTS.md、README.md、docs/01-requirements.md、
docs/02-architecture.md、docs/05-development-plan.md 和 docs/06-tasks.md。
涉及协议时继续读取 docs/10-modbus-register-map.md。

先检查现有文件和 git 状态，保留用户已有改动。
本轮仅完成 T01：环境检查、技术版本锁定、Maven 项目骨架与依赖验证。
不要一次性实现整个项目，也不要预先勾选其他任务。

我负责设计、推理和回答问题，你负责编码、运行测试和解释结果。
先说明本轮范围、已有约束和验证目标，补足必要的工业背景后每次只问一个新场景问题，等待我回答，
不要提前给完整答案；按 docs/05-development-plan.md 提示、补正，设计讨论通过后再实现。
完成后结合实际代码解释调用链；让我先预测一个边界场景，再由你运行验证，
由我解释结果，最后通过一个未直接讲过的变式问题检查理解。
记录命令、退出结果和未验证事项；工程完成与学习通过分别判定。
更新 docs/06-tasks.md 的任务状态及交接记录，
更新 docs/11-decisions.md 的版本锁定表。
保留我的首次回答和是否得到提示，辅助补正单独记录，不覆盖初答。
当前任务工程完成、学习通过前，不进入下一任务；不要求我编写可执行代码。

若依赖下载或权限受阻，不要假装成功，不要绕过限制；
说明阻塞点、已经完成的工作以及最小下一步。
```

## 3. 后续会话继续执行

```text
继续这个项目。先读取 AGENTS.md、docs/05-development-plan.md 和
docs/06-tasks.md 的最新交接，核对实际代码、测试结果及工程/学习两种状态。
我已有 7 年 Java 互联网开发经验，正在补工业领域知识，目标是工业软件 Java 后端。
优先继续当前任务尚未通过的学习环节；先补必要工业背景，再从已有待答问题继续，等我回答。
Java 基础按需复习，已跳过的工具链题不重新设门槛；拓展知识不变成新增工程模块。
工程已完成时不重复实现，不因代码或测试通过就认定我已掌握。
我负责设计、推理和回答，你负责编码与运行验证；每次只问一个问题，
保留首次回答，先提示或解释再补正，最后用新场景验收，不要求我亲写代码。
纯概念练习明确没有运行证据，不要求提前实现后续或范围外功能；工程检查仍完整执行。
当前任务的面试练习使用新场景，已讲过的例题只算复习，T20 汇总讲述。
只有当前任务工程 DONE、学习通过，才进入下一个依赖满足的 P0 任务，
并从设计讨论开始；一次只推进一个任务。
若任务清单与代码不一致，先说明并修正状态，不要把计划当作实现事实。
结束时分别更新工程状态、学习状态、首次回答、补正、验证证据和下一轮入口。
```

## 4. 做阶段验收时发给 Codex

```text
本轮只做验收，不新增功能。
按 docs/07-test-plan.md 检查当前里程碑，
逐项给出已通过、失败、未执行以及相应证据。
不要用 Mock 测试替代真实 Modbus TCP 或 MySQL 集成验证。
不能运行的测试说明环境原因。先报告问题，不擅自扩大范围修改代码。
工程验收与学习验收分别报告，不把测试通过作为我的学习通过证据。
```

## 5. 你需要知道的默认选择

- Java 17；Spring Boot 单体后端；MySQL；真实 Modbus TCP 读寄存器。
- 一个独立 Java 模拟器进程，模拟 3 台 AGV；不是 3 个业务微服务。
- 简单 HTML/CSS/JavaScript 页面，后端通过 SSE 单向推送；第一版不引入 Vue 构建链。
- 第一版只监控，不控制真实设备，不实现搬运任务下发、路径规划或完整 MES/WMS。
- T01 已验证版本解析、Java 17 构建、依赖初始化及 HTTP 启动；真实 Modbus/MySQL 通信和性能尚未验证。详见设计决策和任务交接。
- 学习优先：先讲必要工业背景，再由你设计和预测，Codex 编码与验证；提问时等待回答，按实际理解与证据决定是否进入下一任务。

如果你想换成已有 Spring Boot 项目、Python 模拟器或 Vue 页面，先让 Codex记录影响并同步文档，不要只改其中一个文件。

## 6. 文档导航

| 文件 | 用途 |
| --- | --- |
| [AGENTS.md](AGENTS.md) | Codex 的项目执行规则 |
| [README.md](README.md) | 项目目标、当前状态、阅读入口 |
| [需求](docs/01-requirements.md) | 范围、默认参数、验收门槛 |
| [架构](docs/02-architecture.md) | 工业全景、补料案例、协议学习边界及 Demo 数据链路 |
| [数据模型](docs/03-data-model.md) | 表结构、约束、索引和保存策略 |
| [接口](docs/04-api-design.md) | REST、SSE、模拟器调试接口 |
| [开发与学习计划](docs/05-development-plan.md) | 学习流程、分工、验收与参考工期 |
| [任务清单](docs/06-tasks.md) | 工程/学习状态、任务依赖、回答及验证记录 |
| [测试计划](docs/07-test-plan.md) | 单元、集成、故障注入和手工验收 |
| [部署](docs/08-deployment.md) | 目标命令、Docker Compose、WSL2 注意事项 |
| [面试准备](docs/09-interview-guide.md) | 贯穿开发的工业场景训练、讲述与真实能力边界 |
| [寄存器表](docs/10-modbus-register-map.md) | Modbus 数据协议的唯一事实来源 |
| [设计决策](docs/11-decisions.md) | 选型、版本锁定、变更和官方参考资料 |
